{
  "name": "@vertexvis/vertex-api-client",
  "version": "0.4.1",
  "description": "The Vertex platform API client for TypeScript and JavaScript.",
  "license": "MIT",
  "author": "Vertex Developers <support@vertexvis.com> (https://developer.vertexvis.com)",
  "homepage": "https://github.com/Vertexvis/vertex-api-client-ts#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Vertexvis/vertex-api-client-ts.git"
  },
  "bugs": {
    "url": "https://github.com/Vertexvis/vertex-api-client-ts/issues"
  },
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "files": [
    "dist/",
    "!dist/**/__tests__",
    "LICENSE"
  ],
  "keywords": [
    "vertexvis",
    "api-client",
    "typescript"
  ],
  "scripts": {
    "build": "tsc --outDir dist/",
    "clean": "rm -rf dist/",
    "clean-build": "yarn clean && yarn build",
    "format": "prettier --write './**/*.+(js|jsx|ts|tsx|json|yml|yaml|md|mdx|html|css)'",
    "generate": "yarn clean && ./scripts/generate.sh && yarn format",
    "test": "jest",
    "test:func": "yarn clean-build && node dist/functional-test.js",
    "verify": "yarn clean-build && yarn test && node dist/verify.js",
    "watch": "jest --watch"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "flatted": "^3.1.0",
    "p-limit": "^3.1.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.10",
    "husky": "^4.3.0",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.2",
    "prettier": "^2.2.0",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "**/*": [
      "yarn format"
    ]
  }
}
